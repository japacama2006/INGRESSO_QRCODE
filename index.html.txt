<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Ingresso Digital – Evento TESTE</title>
  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(180deg, #004B8D 0%, #007BFF 100%);
      color: #fff;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      overflow: hidden;
    }
    h1 {
      font-size: 2.5em;
      margin-bottom: 0.3em;
    }
    h2 {
      margin: 0.2em 0;
      font-weight: 400;
    }
    .evento-info {
      background: rgba(255,255,255,0.1);
      padding: 1em 2em;
      border-radius: 20px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
      margin-bottom: 1em;
    }
    .contador {
      background: rgba(255,255,255,0.15);
      padding: 0.5em 1em;
      border-radius: 12px;
      font-size: 1.1em;
    }
    .confete {
      position: absolute;
      width: 100%;
      height: 100%;
      pointer-events: none;
      background-image: radial-gradient(circle at 10% 10%, rgba(255,0,0,0.3) 2px, transparent 2px),
                        radial-gradient(circle at 90% 20%, rgba(255,255,0,0.3) 2px, transparent 2px),
                        radial-gradient(circle at 40% 80%, rgba(0,255,255,0.3) 2px, transparent 2px);
      background-size: 100px 100px;
      animation: move 20s linear infinite;
      z-index: 0;
    }
    @keyframes move {
      from {background-position: 0 0, 100px 0, 50px 50px;}
      to {background-position: 0 100px, 100px 200px, 50px 150px;}
    }
    .conteudo {
      position: relative;
      z-index: 1;
    }
    footer {
      position: absolute;
      bottom: 10px;
      font-size: 0.9em;
      color: rgba(255,255,255,0.8);
    }
  </style>
</head>
<body>
  <div class="confete"></div>
  <div class="conteudo">
    <h1>🎟️ Evento TESTE</h1>
    <div class="evento-info">
      <h2><strong>Local:</strong> Ilha do Pavão</h2>
      <h2><strong>Data:</strong> 23/11</h2>
      <h2><strong>Horário:</strong> 08:30</h2>
    </div>
    <p>Apresente este ingresso no dia do evento.</p>
    <p>Seja bem-vindo(a) ao Grêmio Náutico União!</p>
    <div class="contador">Visualizações: <span id="visitas">0</span></div>
  </div>
  <footer>© 2025 Grêmio Náutico União – Evento TESTE</footer>

  <script>
    // Contador usando countapi.xyz
    const namespace = "gremio_teste_evento";
    const key = "ingresso_digital";
    const url = `https://api.countapi.xyz/hit/${namespace}/${key}`;
    fetch(url)
      .then(res => res.json())
      .then(data => {
        document.getElementById("visitas").textContent = data.value;
      })
      .catch(() => {
        document.getElementById("visitas").textContent = "—";
      });
  </script>
</body>
</html>
